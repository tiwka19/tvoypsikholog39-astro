---
import { Image } from 'astro:assets';
import heroImage from '../assets/hero.png';
---

<section class="hero relative">
  <div class="container mx-auto px-5 pb-10">
    <div class="flex flex-col md:flex-row items-center relative z-50">
      <div class="max-w-3xl">
        <h1 class="text-3xl md:text-4xl xl:text-6xl mb-5">
          Не оставайся один на один со своими проблемами
        </h1>
        <p class="text-xl lg:text-2xl font-body mb-5 sm:mb-10">
          Для нежных и трепетных, для всех мягких сердцем
        </p>
        <a
          href="#"
          class="font-body w-full sm:w-auto inline-block text-center px-10 text-xl md:text-2xl py-3 bg-primary hover:rounded-3xl rounded-tl-3xl rounded-br-3xl transition-all mb-5 md:mb-0"
          >Связаться</a
        >
      </div>
      <div class="relative hero-image -z-10">
        <Image src={heroImage} format="webp" quality={100} alt="hero" />
      </div>
    </div>
  </div>
</section>

<style>
  .hero::after {
    content: '';
    position: absolute;
    top: -120px;
    background: url('../assets/hero-line.svg') 0 0 no-repeat;
    height: 440px;
    width: 100%;
    z-index: 49;
    pointer-events: none;
    left: 0;
  }
  @media (max-width: 768px) {
    .hero::after {
      top: -200px;
    }
  }
  .hero-image::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translate(-50%);
    background: url('../assets/hero-mask.svg') no-repeat;
    background-position: center;
    background-size: cover;
    width: 1200px;
    border-radius: 40%;
    height: 440px;
    z-index: -1;
  }
</style>
